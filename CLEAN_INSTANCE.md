# نسخة الجراح الصفرية

هذه النسخة مخصصة لخدمة مستقلة باسم `whatsapserver-clean`، ولا تستخدم بيانات `whatsapserver-2` أو قرصها الدائم. عند إنشائها بقرص جديد تبدأ قاعدة SQLite وسجل WhatsApp فارغين.

## ما يبدأ فارغًا

لا يتم نسخ أي كباتن أو مستخدمين تشغيليين أو طلبات أو محافظ أو تسويات أو سجل رسائل أو جلسة WhatsApp. ينشئ الخادم حساب شركة تقنيًا واحدًا فقط عند التشغيل لأن دفتر الشركة يحتاج مالكًا داخليًا لتسجيل القيود؛ هذا ليس حساب كابتن ولا مستخدم تشغيل.

## الإعداد الأول

استخدم `render.clean.yaml` عند إنشاء خدمة Render جديدة من الفرع `clean-instance`. لا تضع `BOT_PHONE` أو `BOT_PHONE_INTL` أو `WHATSAPP_GROUP_ID` قبل ربط الرقم والقروب الجديدين. بعد إنشاء الخدمة، أضف القيم التالية من إعدادات البيئة:

- `BOT_PHONE`: الرقم المحلي الجديد عند الحاجة للعمليات التي تعتمد على الرقم.
- `BOT_PHONE_INTL`: الرقم الدولي الجديد.
- `WHATSAPP_GROUP_ID`: معرّف القروب الجديد المنتهي بـ `@g.us`.
- `GROUP_SETUP_OWNER_PHONES`: أرقام المالكين المسموح لهم باعتماد القروب، مفصولة بفواصل.
- `PUBLIC_BASE_URL`: رابط الخدمة الجديدة.

بعد ذلك امسح QR مرة واحدة من `/qr` أو `/qr-main` باستخدام رمز الإدارة، ثم تحقق من `/health` و`/status`. لا تنقل مجلد `.wwebjs_auth` أو ملف `aljarah.sqlite` من الخدمة القديمة.

## الحماية

- `CLEAN_INSTANCE=true` يعطل الأرقام والقروب الافتراضيين القديمين.
- القرص الجديد مستقل عن قرص الخدمة القديمة.
- لا يوجد مسار لتصفير أو حذف بيانات الخدمة القديمة.
- يجب تغيير القيم السرية التي ينشئها Render وفق سياسة الحساب قبل الاستخدام الفعلي.
